services:
  react-app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: react_frontend
    ports:
      - "3000:80" # Приложение будет доступно на http://localhost:3000
    restart: unless-stopped